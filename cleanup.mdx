---
title: "Auditoria & Limpeza"
description: "Lista por repositório de código/ficheiros a limpar — verificado."
---

Lista **verificada** por scan ao código. Legenda de confiança:
🟢 remover (confirmado sem referências) · 🟡 rever (routing por ficheiro / a confirmar) · 🔵 infra/processo.

## Contrato Frontend ↔ API: limpo ✅
21 chamadas `/api/v1/...` dos frontends cruzadas com as rotas definidas: **0 apontam para rotas inexistentes**.

## Ficheiros lixo / temporários 🟢
| Repo | Ficheiro |
|---|---|
| API-PAYMENTS | `.DS_Store` |
| admin-dashboard | `tsconfig.tsbuildinfo` (deve estar no `.gitignore`) |
| konecta.pt | `src/lib/_table.tmp` |
| payments-konecta-web | `.next/`, `out/`, `.vercel/`, `.wrangler/` commitados (artefactos de build — gitignore) |

## konecta.pt (site) 🟢
Confirmados **sem referências** (0 imports):
- `src/pages/Status.tsx` — **página morta** (a rota `/status` usa o `Help`, não esta).
- `src/components/NavLink.tsx`
- `src/components/home/FeaturedOffers.tsx`
- `src/components/home/PromoBanner.tsx`

## App (mobile) 🟢
Código morto confirmado (0 usos — debug/test/old/exemplos):
- `src/components/common/OnboardingScreenOld.tsx`
- `src/components/common/OnboardingScreenNew.tsx`
- `src/components/common/TestR2Assets.tsx`
- `src/components/common/SupabaseDebug.tsx`
- `src/components/common/ParticleEffect.tsx`
- `src/utils/debugAuth.ts`
- `src/examples/dualDatabaseUsage.ts`

🟡 A rever (Expo Router usa `app/` por convenção — não são órfãos): `HomeScreen`,
`KonectaLogo/KonectaLogoSVG`, `VPNMetricsCard`, `DeviceCompatibilityBanner`,
`ThemeToggle`, `Screen`, `GlobeIcon`, `useAssets`, `instantNotificationService`,
`useUserProfileStore` — verificar uso real antes de mexer.

## payments-konecta-web 🟡
- **Build falhado** (OneUptime) → corrigir primeiro.
- Muitos componentes aparecem **não referenciados** pelas páginas (`ErrorModal`,
  `MBWayPaymentForm`, `CardPaymentForm`, `PaymentMethodSelector`,
  `ThreeDSecureModal`, `SuccessModal`, `PlanSummary`, `SecurityBadge`,
  `CheckoutHeader`, `PaymentMethodCard`, `TestCards`) — confirmar contra a página de
  checkout antes de remover. Provável legado.
- Remover artefactos commitados (ver acima).

## admin-dashboard 🟢
- Sem componentes órfãos. Só o `tsconfig.tsbuildinfo` (lixo).

## Backends & Infra 🔵
- **`konecta-status`** — o worker já **não existe** na Cloudflare. Falta só apagar a
  pasta `API-BACKEND/workers-status` do repositório.
- **Rate limiting desligado**: o `rateLimiter()` do API-BACKEND deixa passar tudo
  porque o `UPSTASH_REDIS_URL`/`TOKEN` não estão configurados. Ou se configura o
  Upstash, ou se remove o middleware e as vars `RATE_LIMIT_*` que sugerem proteção
  que não existe.
- **`RESEND_API_KEY` em falta** no projeto Pages do admin → o envio de emails do
  dashboard devolve 503.
- **`ESIMACCESS_ACCESS_CODE`** continua configurado no `konecta-esims` depois de o
  provider ter sido removido → `wrangler secret delete ESIMACCESS_ACCESS_CODE`.
- **`CLOUDFLARE_API_TOKEN`** guardado no projeto Pages do admin sem ser usado pelo
  código das Functions → confirmar e remover.
- **Variáveis com tab no nome** no projeto Pages do site: `VITE_API_URL\t` e
  `VITE_SUPABASE_URL\t` nunca são lidas pelo Vite.
- **Branches obsoletas**: `develop` (API-BACKEND, API-ESIMS, API-PAYMENTS, App),
  `feat/cloudflare-workers-migration` (3 APIs), `dependabot/*` (konecta.pt) → apagar.
- **Vulnerabilidades Dependabot** em vários repos → `npm audit fix` / Dependabot updates.
- **LB**: migrar para domínio isolado (`lb.statuskonecta.pt`); sanar fallbacks
  `api-esims` (503) e `api-backend` (000).

## Base de dados 🔴

Verificado contra o projeto Supabase real (ver [Base de dados](/database)):

- **Três tabelas consultadas em produção não existem**: `competitor_prices`
  (API-BACKEND, página Concorrência), `visitor_sessions` e `visitor_page_views`
  (API-ESIMS, `POST /api/v1/track`). As migrations existem por aplicar — ou se
  aplicam, ou se remove o código.
- Funções e triggers `*_livemap_*` na base de dados sem nenhum frontend que os use.

## Resolvido — não era problema

- **O webhook Stripe não está duplicado.** Só existe um endpoint registado na
  Stripe (`payments.konecta.pt/api/v1/webhooks/stripe`, live, 3 eventos). O
  `/api/v1/webhooks/stripe` do API-BACKEND é o passo interno seguinte, autenticado
  por `INTERNAL_WEBHOOK_SECRET`. Nada a remover.

## Feito

- **eSIM Access removido** (julho 2026) — não vendia nem alimentava o catálogo. Saiu
  `providers/esimaccess.ts`, o bloco em `/prices/compare-providers`, o
  `ESIMACCESS_ACCESS_CODE` e a opção de sync no admin. O helper de câmbio USD→EUR
  passou para `pricing.ts` (`getUsdToEur`) porque o esimcard e o CMLink precisam
  dele. Falta apagar o secret: `wrangler secret delete ESIMACCESS_ACCESS_CODE`.
- **Typecheck do API-ESIMS reposto a zero** (28/07/2026) — eram 39 erros que
  bloqueavam o `ci.yml` e o `deploy-workers.yml`, obrigando a `wrangler deploy`
  manual. Corrigido com `lib: ["ESNext", "DOM"]` no `tsconfig.json`,
  `querySelector<HTMLInputElement>` e a remoção de um import morto em `cmlink.ts`.
  O deploy automático deste worker volta a funcionar.

## Limitações conhecidas (não são bugs)
- Mapa da Overview: 209/210 países pintam; **Kosovo** não pinta por ausência no mapa base.
