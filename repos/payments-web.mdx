---
title: "payments-konecta-web"
description: "Checkout web — Next.js."
---

<Info>
package `konecta-payments` · **Next.js** (React) em Cloudflare Pages (`pay.konecta.pt`).
Scripts: `dev`, `build`, `pages:build`, `pages:deploy`.
</Info>

## Papel

Página de **checkout** web (fluxo de pagamento do cliente). Liga ao
[API-PAYMENTS](/repos/api-payments) (Stripe / MB WAY).

## Nota de estado

No OneUptime, este repo apareceu com **"Build failed"** — confirmar o build antes de
promover a produção (candidato a verificar). Deploy via `pages:build` + `pages:deploy`.
